CREATE TABLE character (
                                 id SERIAL PRIMARY KEY,
                                 name VARCHAR(255) NOT NULL,
                                 movie VARCHAR(255) NOT NULL
);